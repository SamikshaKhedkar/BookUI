import 'package:flutter/material.dart';

class MyTabApp extends StatelessWidget {
  final Color color;
  final String text;
  const MyTabApp({super.key, required this.color, required this.text});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
